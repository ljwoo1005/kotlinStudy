<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        스타일 리소스는 style 태그로 등록
        스타일 속성은 View에 설정되는 여러 속성을
        스타일에 등록하여 한꺼번에 적용할 수 있다.
    -->
    <style name="MyTextStyle">
        <item name="android:textSize">@dimen/size20dp</item>
        <item name="android:textColor">@color/red</item>
    </style>

    <!--
        style 태그에 name 속성으로 식별자를 부여한 후,
        parent 속성으로 name 속성값을 넣어 style 상속이 가능하다.
    -->
    <style name="MyTextStyleSub" parent="MyTextStyle">
        <item name="android:textSize">@dimen/size30dp</item>
        <item name="android:textColor">#0000FF</item>
        <item name="android:background">@color/green</item>
    </style>

</resources>